import { createStyles, makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => createStyles({
  typograhpyBox: {
    width: "100%",
  },
  toggleBox: {
    marginBottom: "10px",
    marginTop: "10px",
  },
  buttonClass: {
    width: "7rem",
  },
}));
